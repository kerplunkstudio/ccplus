name: merge-cleanup
description: Commits worktree changes, cherry-picks to main, resolves conflicts, removes worktree, and prunes branch.
model: sonnet
tools:
  allowed: [Bash, Read, Grep, Glob, Edit, Write]
  blocked: []
